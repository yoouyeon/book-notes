---
book: SQLD
status: draft
date:
title: "관리 구문"
category: ["SQLD"]
---

## DML

- `INSERT`
- `DELETE`
- `MERGE`

SQL 명령어

- `INSERT`
- `UPDATE`

## TCL

### `ROLLBACK`

`ROLLBACK`

SQL 문에서 `ROLLBACK TO SV2` 명령어를 수행하면 `SAVEPOINT SV2` 지점까지 변경된 것을 모두 취소한다.

## DDL

- `TRUNCATE`

SQL 명령어

- `CREATE`
- `ALTER`

### 기존 테이블에 외래키 제약조건을 추가하는 방법

```sql
ALTER TABLE 테이블명 ADD CONSTRAINT 제약조건명 FOREIGN KEY(컬럼) REFERENCES 참조할테이블(컬럼);
```

### `ON DELETE CASCADE`

외래키에 `ON DELETE CASCADE`를 지정하면, 참조하는 부모 테이블이 삭제될 때 자식 테이블의 관련 행도 연쇄적으로 삭제된다.
